# Install torch first, matched to your CUDA driver:
#   uv pip install --index-url https://download.pytorch.org/whl/cu128 torch
torch>=2.1
numpy
matplotlib    # figures only; prove.py does not need it
